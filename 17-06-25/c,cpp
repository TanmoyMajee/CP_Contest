
#include <bits/stdc++.h>
using namespace std;

void solve()
{
  int n , m;
  cin >> n >> m;
  int arr[n][m];
  for(int i=0;i<n;i++){
    for(int j=0;j<m;j++)
      cin>>arr[i][j];
  }
  // now apply algo
  int MX = 0;
  // hash
  vector< pair<int,int> > vec(101,{-1,-1});

  for(int i=0;i<n;i++){
    for (int j = 0; j < m; j++){
        // if its alrady assign row or col then assign to {-1,-1}
        int val = arr[i][j];
        int r = vec[val].first; int c =vec[val].second;
        if(r!=-1 )
    }
  }
}

int main()
{
  int t;
  cin >> t;
  // cin.ignore();
  while (t--)
  {
    solve();
  }
  return 0;
}
